import 'package:flutter/material.dart';

class ResponsableProfile extends StatelessWidget {
  const ResponsableProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}