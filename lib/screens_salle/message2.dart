

import 'package:flutter/material.dart';

class ResponsableMessage extends StatelessWidget {
  const ResponsableMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}